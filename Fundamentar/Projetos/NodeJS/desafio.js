/**
 * crie 2 arquivos JacaScript
 * O primeiro, ira exportar uma função chamada getFlag()
 * que receberá um argumento do tipo string
 * Essa função deverá buscar dentro do array process.argv e flag desejada que é a String
 *  e retornar o valor da flag.
 * 
 * o Segundo irá importar a função e passar a flag desejada
 * 
 * Iremos rodar no terminal o segundo arquivo passando as flags --name e --greeting
 * para que seja impresso no terminal a saudaçã e o nome da pessoa.
 */