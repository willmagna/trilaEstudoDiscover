//Operador condicional (ternário)

//depedendo da condição, nos receberemos valores diferetes

//condição então valor 1 se nao valor 2
//condição ? value1 : value2
//condição ? isso   : se nao isso

//Exemplos

//café da manhça top
let pao = true
let queijo = false

const niceBreakFast = pao && queijo ? `Café top` : `Cafe não top`

console.log(niceBreakFast)

//maior de 18